title = "ha-aria gitleaks config"
[extend]
useDefault = true
[[rules.allowlists]]
description = "Test fixture credentials"
regexes = ["test[-_]token","fake[-_](key|token|secret|api)","example[-_](key|token)","<ha-host>","<mqtt-broker-ip>"]
paths = ["tests/",".env.example"]
